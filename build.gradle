apply plugin: 'java'
apply plugin: 'maven'
apply plugin: 'eclipse'

group = 'sc.fiji'
version = '2.1.7-PF-PATCH'

description = """plugins/Trainable_Segmentation.jar"""

sourceCompatibility = 1.5
targetCompatibility = 1.5

configurations.all {
}

repositories {
        
     maven { url "http://maven.imagej.net/content/groups/public" }
     maven { url "http://repo.maven.apache.org/maven2" }
}
dependencies {
    compile group: 'sc.fiji', name: 'Anisotropic_Diffusion_2D', version:'2.0.0'
    compile group: 'sc.fiji', name: 'VIB-lib', version:'2.0.1'
    compile group: 'sc.fiji', name: 'fiji-lib', version:'2.1.0'
    compile group: 'sc.fiji', name: 'imagescience', version:'2.4.2'
    compile group: 'net.imglib2', name: 'imglib2', version:'2.2.0'
    compile group: 'net.imglib2', name: 'imglib2-ij', version:'2.0.0-beta-28'
    compile group: 'net.imglib2', name: 'imglib2-algorithm-gpl', version:'0.1.2'
    compile group: 'net.imagej', name: 'ij', version:'1.49m'
    compile group: 'nz.ac.waikato.cms.weka', name: 'weka-dev', version:'3.7.11'
    compile group: 'org.apache.commons', name: 'commons-math3', version:'3.2'
    testCompile group: 'junit', name: 'junit', version:'4.11'
    compile(group: 'java3d', name: 'j3d-core', version:'1.5.2') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'java3d', name: 'j3d-core-utils', version:'1.5.2') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'java3d', name: 'vecmath', version:'1.5.2') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
